replicaCount: 2

image:
  repository: REPLACE_WITH_QA_ECR_REPOSITORY
  tag: "REPLACE_WITH_IMAGE_TAG"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

containerPort: 8080

resources:
  requests:
    cpu: 25m
    memory: 32Mi
  limits:
    cpu: 200m
    memory: 128Mi
