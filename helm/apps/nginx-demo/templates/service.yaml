apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-demo.name" . }}
  labels:
    {{- include "nginx-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "nginx-demo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
